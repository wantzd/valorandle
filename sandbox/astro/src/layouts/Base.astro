---
interface Props {
  title: string;
  description: string;
  lang: 'pt-BR' | 'en';
  path: string;
  extraCss?: string;
  noindex?: boolean;
}

const { title, description, lang, path, extraCss, noindex = false } = Astro.props;

const canonical = `https://valorandle.com${lang === 'en' ? '/en' : ''}${path}`;
const hreflangPT = `https://valorandle.com${path}`;
const hreflangEN = `https://valorandle.com/en${path}`;
const ogLocale = lang === 'en' ? 'en_US' : 'pt_BR';
const ogLocaleAlt = lang === 'en' ? 'pt_BR' : 'en_US';

const schemaDescription = lang === 'en'
  ? "Valorandle is the VALORANT Wordle: guess the VCT pro player with up to 8 tries. New challenge every day with players from all 4 global leagues."
  : "Valorandle é o Wordle de VALORANT: adivinhe o pro player do VCT com até 8 tentativas. Novo desafio todo dia com jogadores das 4 ligas globais.";

const schemaJson = JSON.stringify({
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "Valorandle",
  "alternateName": ["Valorant Wordle", "Valdle"],
  "description": schemaDescription,
  "url": `https://valorandle.com${path}`,
  "applicationCategory": "GameApplication",
  "genre": "Puzzle",
  "operatingSystem": "Web",
  "inLanguage": ["pt-BR", "en"],
  "isAccessibleForFree": true,
  "offers": { "@type": "Offer", "price": "0", "priceCurrency": "BRL" },
  "image": "https://valorandle.com/assets/icons/app-icon-512.png"
});
---
<!DOCTYPE html>
<html lang={lang}>
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <meta name="keywords" content="valorandle, valdle, valorant wordle, vct pro player, adivinhe o jogador valorant, valorant quiz" />
  {noindex ? <meta name="robots" content="noindex, follow" /> : <meta name="robots" content="index, follow" />}
  <meta name="author" content="Valorandle" />

  <!-- Canonical + hreflang -->
  <link rel="canonical" href={canonical} />
  <link rel="alternate" hreflang="pt-BR" href={hreflangPT} />
  <link rel="alternate" hreflang="en" href={hreflangEN} />
  <link rel="alternate" hreflang="x-default" href={hreflangPT} />

  <!-- Open Graph -->
  <meta property="og:type" content="website" />
  <meta property="og:url" content={canonical} />
  <meta property="og:site_name" content="Valorandle" />
  <meta property="og:locale" content={ogLocale} />
  <meta property="og:locale:alternate" content={ogLocaleAlt} />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:image" content="https://valorandle.com/assets/icons/app-icon-512.png" />

  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={title} />
  <meta name="twitter:description" content={description} />
  <meta name="twitter:image" content="https://valorandle.com/assets/icons/app-icon-512.png" />

  <meta name="theme-color" content="#FF4655" />

  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="/assets/icons/favicon.svg" />
  <link rel="icon" type="image/png" sizes="64x64" href="/assets/icons/favicon-64.png" />
  <link rel="icon" type="image/png" sizes="32x32" href="/assets/icons/favicon-32.png" />
  <link rel="apple-touch-icon" sizes="180x180" href="/assets/icons/apple-touch-180.png" />
  <link rel="manifest" href="/manifest.json" />

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Russo+One&family=Epilogue:wght@400;600;700&family=Montserrat:wght@500;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flag-icons@7.2.3/css/flag-icons.min.css" />

  <!-- Schema.org -->
  <script type="application/ld+json" set:html={schemaJson} />

  {extraCss && <style is:global set:html={extraCss} />}
  <style is:global>
    :root { --font-ui: 'Epilogue', sans-serif !important; --font-mono: 'Epilogue', sans-serif !important; }
  </style>
</head>
<body>
  <slot />
</body>
</html>
